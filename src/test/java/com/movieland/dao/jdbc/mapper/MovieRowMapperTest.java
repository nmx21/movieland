package com.movieland.dao.jdbc.mapper;

import com.movieland.entity.Movie;
import junit.framework.TestCase;
import org.junit.Ignore;
import org.junit.Test;

import java.sql.ResultSet;
import java.sql.SQLException;

import static org.junit.Assert.assertEquals;
import static org.mockito.Matchers.any;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class MovieRowMapperTest {
    @Test
    @Ignore
    public void testMapRowWithProperMovie() throws SQLException {
//        ResultSet resultSet = mock(ResultSet.class);
//
//        when(resultSet.getInt(any())).thenReturn(1).thenReturn(1000);
//        when(resultSet.getString(any())).thenReturn("Test name").thenReturn("Test district");
//
//        MovieRowMapper movieMapper = new MovieRowMapper();
//        Movie actualMuvie = movieMapper.mapRow(resultSet, 0);
//        System.out.println(actualMuvie.getId());
//        //assertEquals(actualMuvie.getId(), 1);
    }
}